spring:
  application:
    name: SEARCH-SERVICE 
    cloud:    stream:      bindings:         output:
           destination: kafka-stream

eureka:
 client:
    register-with-eureka: true
    fetch-registry: true
 
   
    service-url:
      defaultZone: http://localhost:8761/eureka
      
 instance:
   hostname: localhost
   